- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~', '~/.config']

- link:
    ~/.zshrc:
    ~/.vscode:
        glob: true
        path: vscode/*
    ~/.config/:
        glob: true
        path: config/*
